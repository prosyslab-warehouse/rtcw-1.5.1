# /usr/lib is the conventional place to put a non-FHS tree that uses
# symlinks to pull in architecture-independent files from /usr/share
# or configuration from /etc (#971707)
breakout-link usr/lib/rtcw/main/etc/rtcw-server -> etc/rtcw-server
